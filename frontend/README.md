# Capital Markets Frontend

## Setup
```bash
npm install
npm run dev
```

## Requirements
- Spring Boot backend must be running on http://localhost:8080
- MySQL must be running with all V1-V5 migrations applied
- Default trader ID: 1 (change in orderStore.js → currentTraderId)
